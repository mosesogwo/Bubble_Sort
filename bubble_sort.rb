def swap(arr, source, destination)
  temp = arr[source]
  arr[source] = arr[destination]
  arr[destination] = temp
end

def bubble_sort(arr)
  n = arr.length
  # i = 0
  # while (i < n)
  for element in arr
    p element
  end
end

p bubble_sort([1, 2, 3])
